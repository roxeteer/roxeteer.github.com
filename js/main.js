(function($) {
    'use strict';

    $('main').fullpage({
        scrollOverflow: true,
        css3: true,
        anchors: ['top', 'skills'],
        slidesColor: ['#554a4b'],
        resize: false
    });
})(jQuery);
